import PracticeCard from '@/features/practice/PracticeCard';
import type { JSX } from 'react/jsx-dev-runtime';

/**
 * Renders the Practice page component, which displays a PracticeCard for English practice activities.
 *
 * @returns {JSX.Element} The JSX element representing the Practice page.
 */
export default function Practice(): JSX.Element {
  return <PracticeCard />;
}
